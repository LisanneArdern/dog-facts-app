import './App.css'
// import dogFacts from './data.json'

export default function App() {
  return (
    <div className="App">
     {/* {dogFacts.map(dogFact => {
        const { fact, id } = dogFact
        return (
          <div key={id}>{fact}</div>
            
            
            
         
        )
      })} */}
    </div>
  )
}
